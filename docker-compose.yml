version: "3.9"
services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
  worker:
    build: ./backend
    command: celery -A app.worker.celery_app worker --loglevel=info
  redis:
    image: redis:7-alpine
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
